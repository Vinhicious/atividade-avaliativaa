#include <stdio.h>
#include <math.h>

int main(){
float x, calculo;

  printf("isira o x: ");
  scanf("%f", &x);

  calculo = (5 * x + 3) / (sqrt(x) - 16);

  printf("Resultado: %.2f", calculo);
 
  
}
